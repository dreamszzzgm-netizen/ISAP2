{# sections/06_composition.j2 #}
{# content_type: data — LLM не вызывается #}
{# slot_type: fact #}
{# data_sources: facility, organization #}

6. СОСТАВ И ДИСЛОКАЦИЯ СИЛ И СРЕДСТВ, ПРИВЛЕКАЕМЫХ ДЛЯ ЛОКАЛИЗАЦИИ И ЛИКВИДАЦИИ АВАРИЙ

6.1. Состав сил и средств

{% if forces_composition %}
| № п/п | Наименование подразделения (формирования) | Место дислокации | Численность, чел. | Основные задачи |
|--------|-------------------------------------------|-----------------|-------------------|-----------------|
{% for unit in forces_composition %}
| {{ loop.index }} | {{ unit.name | default('—') }} | {{ unit.location | default('—') }} | {{ unit.personnel | default('—') }} | {{ unit.tasks | default('—') }} |
{% endfor %}
{% else %}
| № п/п | Наименование подразделения (формирования) | Место дислокации | Численность, чел. | Основные задачи |
|--------|-------------------------------------------|-----------------|-------------------|-----------------|
| — | Данные не предоставлены | — | — | — |
{% endif %}

6.2. Дислокация и маршруты выдвижения

{% if dispatch_routes %}
| № п/п | Подразделение (формирование) | Пункт дислокации | Маршрут выдвижения | Время выдвижения, мин | Расстояние, км |
|--------|------------------------------|-----------------|--------------------|-----------------------|---------------|
{% for route in dispatch_routes %}
| {{ loop.index }} | {{ route.unit | default('—') }} | {{ route.depot | default('—') }} | {{ route.route | default('—') }} | {{ route.time_min | default('—') }} | {{ route.distance_km | default('—') }} |
{% endfor %}
{% else %}
| № п/п | Подразделение (формирование) | Пункт дислокации | Маршрут выдвижения | Время выдвижения, мин | Расстояние, км |
|--------|------------------------------|-----------------|--------------------|-----------------------|---------------|
| — | Данные не предоставлены | — | — | — | — |
{% endif %}

6.3. Резерв сил и средств

{% if reserves %}
| № п/п | Наименование резерва | Количество | Место размещения | Порядок привлечения |
|--------|---------------------|------------|-----------------|---------------------|
{% for reserve in reserves %}
| {{ loop.index }} | {{ reserve.name | default('—') }} | {{ reserve.quantity | default('—') }} | {{ reserve.location | default('—') }} | {{ reserve.deployment_order | default('—') }} |
{% endfor %}
{% else %}
Сведения о резервах сил и средств не предоставлены.
{% endif %}
