{# sections/00_toc.j2 #}
{# content_type: data — содержание документа #}
{# ВАЖНО: раньше этот файл был статичным текстом, не связанным со structure.json —
   заголовки и нумерация не совпадали с реальными разделами документа.
   Теперь шаблон строится из переменной `sections` (список словарей с ключами
   id/title), которую нужно передать в контекст рендера ИМЕННО для этого раздела —
   например: sections=structure["sections"] — при вызове _render_template()
   для section_id == "toc". #}

СОДЕРЖАНИЕ

{% for section in sections -%}
{{ section.title }}
{% endfor %}
