{# 30_appendix_1.j2 — Приложение 1: Порядок изучения ПМЛА #}

## Приложение 1. Порядок изучения Планов местных аварийно-спасательных работ

### 1. Порядок обучения персонала

| N п/п | Категория персонала | Содержание обучения | Форма проведения | Периодичность |
|-------|---------------------|--------------------|-----------------|--------------|
{% for item in training_program | default([]) %}
| {{ loop.index }} | {{ item.category | default('—') }} | {{ item.content | default('—') }} | {{ item.form | default('—') }} | {{ item.frequency | default('—') }} |
{% endfor %}
{% if training_program is not defined or training_program | length == 0 %}
| 1 | Руководящий состав | Изучение ПМЛА, порядок оповещения и руководства работами | Теоретическое занятие | Не реже 1 раза в год |
| 2 | Спасатели (пожарные) | Изучение ПМЛА, порядок действия при получении команды | Практическое занятие | Не реже 1 раза в полугодие |
| 3 | Рабочие и служащие | Действия по сигналу тревоги, эвакуация, первичные меры защиты | Инструктаж на рабочем месте | При приёме на работу и не реже 1 раза в год |
{% endif %}

### 2. Сроки проведения инструктажа

| Вид инструктажа | Сроки проведения | Ответственное лицо |
|----------------|-----------------|-------------------|
{% for item in instruction_schedule | default([]) %}
| {{ item.type | default('—') }} | {{ item.deadline | default('—') }} | {{ item.responsible | default('—') }} |
{% endfor %}
{% if instruction_schedule is not defined or instruction_schedule | length == 0 %}
| Вводный | В день приёма на работу | Начальник отдела охраны труда |
| Первичный на рабочем месте | В течение первых 3 смен (смен — дней) после приёма | Непосредственный руководитель |
| Повторный | Не реже 1 раза в год | Непосредственный руководитель |
| Внеплановый | При изменении условий производства работ, после аварий, при вводе новых стандартов | Непосредственный руководитель |
{% endif %}

### 3. Ответственные лица за организацию изучения ПМЛА

{% for person in responsible_persons | default([]) %}
- **{{ person.position | default('—') }}**: {{ person.name | default('—') }} — {{ person.responsibility | default('—') }}
{% endfor %}
{% if responsible_persons is not defined or responsible_persons | length == 0 %}
- **Директор (руководитель организации)**: {{ director_name | default('—') }} — общая ответственность за внедрение и выполнение ПМЛА
- **Главный инженер**: {{ chief_engineer | default('—') }} — организация изучения ПМЛА инженерно-техническим персоналом
- **Начальник службы промышленной безопасности / охраны труда**: {{ safety_officer | default('—') }} — контроль за проведением инструктажей и обучением персонала
{% endif %}
